/********************************************************
 * MSc C++ Programming Unassessed Exercise 1 - Substring
 * Header File - substring.h
 * Author - Chris Snowden (cps15)
 ********************************************************/


/*Returns true if prefixStr is a prefix of str, false otherwise*/
bool is_prefix(const char* prefixStr, const char* str);

/*Returns the first position of subStr in str. If no found retruns -1*/
int substring_position(const char* subStr, const char* str);


int substring_position2(const char* subStr, const char* str);
